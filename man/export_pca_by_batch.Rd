% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_pca.R
\name{export_pca_by_batch}
\alias{export_pca_by_batch}
\title{Export per-batch PCA and reverse scree PDFs}
\usage{
export_pca_by_batch(
  all_batches_subtracted,
  out_dir = ".",
  na_method = "median",
  base_size = 10,
  file_norm = "PCA_Scree_by_batch_normalized.pdf",
  file_unnorm = "PCA_Scree_by_batch_unnormalized.pdf",
  width = 8.5,
  height = 11
)
}
\arguments{
\item{all_batches_subtracted}{A data frame as returned by
\code{calculate_concentrations()}.}

\item{out_dir}{Character. Base results directory. PDFs are written to
a \code{pca_output/} subfolder. Default \code{"."}.}

\item{na_method}{Character. NA handling: \code{"median"} (impute with
column median) or \code{"drop"} (remove rows with any NA).
Default \code{"median"}.}

\item{base_size}{Numeric. Base font size for plots. Default \code{10}.}

\item{file_norm}{Character. Filename for normalized PDF.}

\item{file_unnorm}{Character. Filename for unnormalized PDF.}

\item{width}{Numeric. PDF width in inches. Default \code{8.5}.}

\item{height}{Numeric. PDF height in inches. Default \code{11}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of
  writing PDFs.
}
\description{
Runs PCA separately for each batch using all replicate types, then
exports two portrait-layout PDFs — one for normalized ug/g and one for
unnormalized ug/g — each containing one page per batch with the PCA
biplot stacked above the reverse scree plot.
}
\details{
Output filenames:
\itemize{
  \item \code{PCA_Scree_by_batch_normalized.pdf}
  \item \code{PCA_Scree_by_batch_unnormalized.pdf}
}


\strong{Insufficient data:} Batches with fewer than 2 analyte features
or 2 replicate rows after filtering are skipped with a placeholder page
noting "insufficient data".

\strong{What to modify:} \code{na_method} to switch between median
imputation and complete-case analysis; \code{out_dir} for output location.
}
\examples{
\dontrun{
export_pca_by_batch(
  all_data,
  out_dir   = "results/",
  na_method = "median"
)
}

}
